services:
  femaction-bot:
    build: .
    image: femaction-bot:latest
    container_name: femaction-bot
    restart: unless-stopped
    volumes:
      - ./config.json:/app/config.json:ro
    environment:
      - NODE_ENV=production
